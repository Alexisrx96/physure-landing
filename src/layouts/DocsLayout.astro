---
export interface Props {
  title: string;
  description?: string;
  activeSection?: string;
}

const { title, description = "MeasureKit — Unit-aware physics computation for Python", activeSection = "" } = Astro.props;

const SITE = Astro.site?.href.replace(/\/$/, "") ?? "";
const OG_IMAGE = SITE + "/logo.jpg";

// Multi-language detection
const isSpanish = Astro.url.pathname.startsWith("/es");
const lang = isSpanish ? "es" : "en";
const langPrefix = isSpanish ? "/es" : "";

// Generate path for the other language switcher
const currentPath = Astro.url.pathname;
const otherLangPath = isSpanish 
  ? (currentPath.replace(/^\/es/, "") || "/") 
  : (`/es${currentPath === "/" ? "" : currentPath}`);

const navItems = [
  { label: isSpanish ? "Inicio" : "Overview", href: `${langPrefix}/`, section: "overview", icon: "🏠" },
  { label: isSpanish ? "Instalación" : "Installation", href: `${langPrefix}/install`, section: "install", icon: "📦" },
  { label: isSpanish ? "Guía Rápida" : "Quick Start", href: `${langPrefix}/quickstart`, section: "quickstart", icon: "⚡" },
  { label: isSpanish ? "Guía de Usuario" : "User Guide", href: `${langPrefix}/guide`, section: "guide", icon: "📖" },
  { label: isSpanish ? "Referencia API" : "API Reference", href: `${langPrefix}/api`, section: "api", icon: "🔧" },
  { label: isSpanish ? "Ejemplos" : "Examples", href: `${langPrefix}/examples`, section: "examples", icon: "🧪" },
  { label: isSpanish ? "Tutoriales" : "Tutorials", href: `${langPrefix}/tutorials`, section: "tutorials", icon: "📚" },
  { label: isSpanish ? "Playground" : "Playground", href: `${langPrefix}/playground`, section: "playground", icon: "🚀" },
  { label: isSpanish ? "CLI y REPL" : "CLI & REPL", href: `${langPrefix}/cli`, section: "cli", icon: "💻" },
  { label: isSpanish ? "Historial" : "Changelog", href: `${langPrefix}/changelog`, section: "changelog", icon: "📋" },
];

// Layout Translations
const t = {
  navigation: isSpanish ? "Navegación" : "Navigation",
  install: isSpanish ? "Instalación" : "Install",
  stats: isSpanish ? "Estadísticas" : "Stats",
  license: isSpanish ? "Licencia" : "License",
  python: "Python",
  version: isSpanish ? "Versión" : "Version",
  status: isSpanish ? "Estado" : "Status",
  deps: "Deps",
  copy: isSpanish ? "Copiar" : "Copy",
};
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} — MeasureKit Docs</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={SITE + Astro.url.pathname} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title + " — MeasureKit Docs"} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={SITE + Astro.url.pathname} />
    <meta property="og:image" content={OG_IMAGE} />
    <meta name="twitter:card" content="summary" />
  </head>

  <body>
    <!-- Top Navigation Bar -->
    <header class="topbar">
      <div class="topbar-inner">
        <a href={langPrefix + "/"} class="brand">
          <img src="/logo.jpg" alt="MeasureKit" width="32" height="32" class="brand-logo" />
          <span class="brand-name">MeasureKit</span>
          <span class="brand-badge">v0.1.8</span>
        </a>
        <button class="search-trigger" id="searchTrigger" title={isSpanish ? "Buscar en la documentación" : "Search documentation"}>
          <span>🔍 {isSpanish ? "Buscar..." : "Search..."}</span>
          <kbd class="search-kbd">Ctrl K</kbd>
        </button>
        <nav class="topbar-nav">
          <a href={otherLangPath} class="lang-switcher">
            {isSpanish ? "🌐 EN" : "🌐 ES"}
          </a>
          <a href="https://github.com/Alexisrx96/measurekit" target="_blank" rel="noopener" class="topbar-link">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            GitHub
          </a>
          <a href="https://pypi.org/project/measurekit/" target="_blank" rel="noopener" class="topbar-link">PyPI</a>
        </nav>
        <button class="menu-toggle" id="menuToggle" aria-label="Toggle menu">☰</button>
      </div>
    </header>

    <div class="layout">
      <!-- Sidebar -->
      <aside class="sidebar" id="sidebar">
        <div class="sidebar-inner">
          <div class="sidebar-section">
            <p class="sidebar-label">{t.navigation}</p>
            <ul class="sidebar-nav">
              {navItems.map((item) => (
                <li>
                  <a
                    href={item.href}
                    class={`sidebar-link ${activeSection === item.section ? "active" : ""}`}
                  >
                    <span class="nav-icon">{item.icon}</span>
                    {item.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          <div class="sidebar-section">
            <p class="sidebar-label">{t.install}</p>
            <div class="install-box">
              <code>pip install measurekit</code>
              <button class="copy-btn" data-copy="pip install measurekit" title={t.copy}>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
              </button>
            </div>
          </div>

          <div class="sidebar-section">
            <p class="sidebar-label">{t.stats}</p>
            <ul class="sidebar-stats">
              <li><span class="stat-key">{t.license}</span><span class="stat-val">MIT</span></li>
              <li><span class="stat-key">{t.python}</span><span class="stat-val">3.10+</span></li>
              <li><span class="stat-key">{t.version}</span><span class="stat-val">0.1.8</span></li>
              <li><span class="stat-key">{t.status}</span><span class="stat-val status-beta">Beta</span></li>
              <li><span class="stat-key">{t.deps}</span><span class="stat-val">0 (base)</span></li>
            </ul>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="main-content">
        <div class="content-inner">
          <slot />
        </div>
      </main>
    </div>

    <!-- Search Modal -->
    <div class="search-modal-backdrop" id="searchModal" style="display: none;">
      <div class="search-modal">
        <div class="search-header">
          <input type="text" id="searchInput" placeholder={isSpanish ? "Buscar en la documentación..." : "Search documentation..."} autocomplete="off" />
          <button class="search-close" id="searchClose">✕</button>
        </div>
        <div class="search-results" id="searchResults"></div>
        <div class="search-footer">
          <span>↑↓ {isSpanish ? "Navegar" : "Navigate"}</span>
          <span>↵ {isSpanish ? "Seleccionar" : "Select"}</span>
          <span>esc {isSpanish ? "Cerrar" : "Close"}</span>
        </div>
      </div>
    </div>

    <script define:vars={{ isSpanish }}>
      // Mobile menu toggle
      const menuToggle = document.getElementById('menuToggle');
      const sidebar = document.getElementById('sidebar');
      menuToggle?.addEventListener('click', () => {
        sidebar?.classList.toggle('open');
      });

      // Copy buttons
      document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          const text = btn.getAttribute('data-copy') || '';
          navigator.clipboard.writeText(text).then(() => {
            const original = btn.innerHTML;
            btn.innerHTML = '✓';
            setTimeout(() => { btn.innerHTML = original; }, 2000);
          });
        });
      });

      // Run in Playground link interception
      document.querySelectorAll('.run-in-playground').forEach(link => {
        link.addEventListener('click', (e) => {
          e.preventDefault();
          const container = link.closest('.step-pane') || link.closest('section') || link.closest('.api-card') || link.closest('.demo-card') || document;
          const codeEl = container.querySelector('pre code');
          if (codeEl) {
            const rawCode = codeEl.textContent;
            const encoded = btoa(unescape(encodeURIComponent(rawCode)));
            const isSp = window.location.pathname.startsWith('/es');
            
            // Build the backlink to resume state
            let fromUrl = window.location.pathname;
            const activeStepPane = link.closest('.step-pane');
            const activeTutorialContent = link.closest('.tutorial-content');
            if (activeStepPane && activeTutorialContent) {
              const step = activeStepPane.getAttribute('data-step');
              const tutorialId = activeTutorialContent.id.replace('Tutorial', ''); // e.g. "sensor"
              fromUrl += `?tutorial=${tutorialId}&step=${step}`;
            }
            
            window.location.href = `${isSp ? '/es' : ''}/playground?code=${encodeURIComponent(encoded)}&from=${encodeURIComponent(fromUrl)}`;
          }
        });
      });

      // Code block copy buttons
      document.querySelectorAll('pre').forEach(pre => {
        const btn = document.createElement('button');
        btn.className = 'pre-copy-btn';
        btn.title = 'Copy code';
        btn.innerHTML = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>';
        pre.style.position = 'relative';
        pre.appendChild(btn);
        btn.addEventListener('click', () => {
          const code = pre.querySelector('code')?.textContent || '';
          navigator.clipboard.writeText(code).then(() => {
            btn.innerHTML = '✓';
            setTimeout(() => {
              btn.innerHTML = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>';
            }, 2000);
          });
        });
      });

      // Search database
      const searchIndex = [
        // English
        { title: "Overview / Home", url: "/", excerpt: "Intro to MeasureKit unit-aware physics calculations.", lang: "en" },
        { title: "What is MeasureKit?", url: "/#what-is-measurekit", excerpt: "Under-the-hood differences with Pint.", lang: "en" },
        { title: "Honest Performance", url: "/#honest-performance", excerpt: "Eager mode speed vs compiled graph optimization.", lang: "en" },
        { title: "Installation", url: "/install", excerpt: "Install with pip or uv for base, native Rust or all backends.", lang: "en" },
        { title: "Building from source", url: "/install#building-from-source", excerpt: "Compiling maturin Rust core from source.", lang: "en" },
        { title: "Quick Start", url: "/quickstart", excerpt: "Create first quantity, convert, uncertainty propagation.", lang: "en" },
        { title: "Grammar notation", url: "/quickstart#grammar-notation", excerpt: "MeasureNote syntax in Python interpreter.", lang: "en" },
        { title: "Terminal calculator", url: "/quickstart#terminal-calculator", excerpt: "CLI options and interactive REPL.", lang: "en" },
        { title: "User Guide", url: "/guide", excerpt: "Full guide covering physical quantities and unit singletons.", lang: "en" },
        { title: "Absolute vs Delta Temperature", url: "/guide#arithmetic", excerpt: "Celsius and Fahrenheit offsets versus delta units.", lang: "en" },
        { title: "Trigonometric & Transcendentals", url: "/guide#transcendentals", excerpt: "Dimensionless sine, exp, log, tanh on quantities.", lang: "en" },
        { title: "Uncertainty Propagation", url: "/guide#uncertainty", excerpt: "Taylor series, Monte Carlo, and Unscented modes.", lang: "en" },
        { title: "Correlated Covariance Store", url: "/guide#covariance", excerpt: "UUID lineages and sparse matrix propagation.", lang: "en" },
        { title: "JAX Stateless Functional API", url: "/guide#functional", excerpt: "FunctionalState pytree tracking for JAX JIT.", lang: "en" },
        { title: "JIT Tracing & Zero-Overhead", url: "/guide#jit", excerpt: "torch.compile dispatching and raw GPU kernels.", lang: "en" },
        { title: "SymPy Symbolic Math", url: "/guide#symbolic", excerpt: "SymbolicQuantity, Equation solving, diff and integrate.", lang: "en" },
        { title: "Pandas Accessor", url: "/guide#pandas", excerpt: "Series extension array and custom measurekit[unit] type.", lang: "en" },
        { title: "Serialization (IO)", url: "/guide#io", excerpt: "HDF5 dataset attributes and xarray accessories.", lang: "en" },
        { title: "API Reference", url: "/api", excerpt: "Quantity properties and methods lookup table.", lang: "en" },
        { title: "Module functions", url: "/api#module-functions", excerpt: "system_context, propagation_mode, jit, save_state.", lang: "en" },
        { title: "Exceptions", url: "/api#exceptions", excerpt: "IncompatibleUnitsError, UnknownUnitError, ConversionError.", lang: "en" },
        { title: "Examples", url: "/examples", excerpt: "Kinematics, Force, Pressure, Temperature, Uncertainty, NumPy, SymPy, JIT.", lang: "en" },
        { title: "CLI & REPL Details", url: "/cli", excerpt: "MeasureNote grammar statements and notes piping.", lang: "en" },
        { title: "Changelog", url: "/changelog", excerpt: "Timeline release notes from v0.1.0 to v0.1.8.", lang: "en" },

        // Spanish
        { title: "Inicio", url: "/es/", excerpt: "Introducción a los cálculos físicos con unidades en Python.", lang: "es" },
        { title: "Rendimiento Real", url: "/es/#honest-performance", excerpt: "Velocidad eager vs optimización con compilación de grafos.", lang: "es" },
        { title: "Instalación", url: "/es/install", excerpt: "Instalación por pip, uv, extras native, numpy, torch, jax.", lang: "es" },
        { title: "Compilar desde código fuente", url: "/es/install#building-from-source", excerpt: "Compilación de maturin Rust core desde repositorio.", lang: "es" },
        { title: "Guía Rápida", url: "/es/quickstart", excerpt: "Uso básico de la factoría Q_, conversiones y REPL.", lang: "es" },
        { title: "Calculadora de consola", url: "/es/quickstart#terminal-calculator", excerpt: "CLI de MeasureKit para evaluación directa.", lang: "es" },
        { title: "Guía de Usuario", url: "/es/guide", excerpt: "Completa guía de arquitectura de MeasureKit.", lang: "es" },
        { title: "Temperaturas Absolutas vs Delta", url: "/es/guide#arithmetic", excerpt: "Diferencia de temperatura en offsets Celsius/Fahrenheit.", lang: "es" },
        { title: "Funciones Trascendentales", url: "/es/guide#transcendentals", excerpt: "Seno, coseno, exp, log en cantidades físicas.", lang: "es" },
        { title: "Propagación de Incertidumbre", url: "/es/guide#uncertainty", excerpt: "Modos Monte Carlo y Transformada Unscented.", lang: "es" },
        { title: "Matriz de Covarianza Global", url: "/es/guide#covariance", excerpt: "Almacén de covarianza y transformaciones afines.", lang: "es" },
        { title: "JAX API Funcional sin Estado", url: "/es/guide#functional", excerpt: "Uso de FunctionalState como pytree para JAX JIT.", lang: "es" },
        { title: "Compilación JIT y Trazado", url: "/es/guide#jit", excerpt: "Integración torch.compile y kernels nativos en GPU.", lang: "es" },
        { title: "Matemática Simbólica SymPy", url: "/es/guide#symbolic", excerpt: "SymbolicQuantity, resolución de ecuaciones, derivadas e integrales.", lang: "es" },
        { title: "Pandas e Integraciones", url: "/es/guide#pandas", excerpt: "Series extendidas y tipo measurekit[m/s].", lang: "es" },
        { title: "Serialización (IO)", url: "/es/guide#io", excerpt: "Formatos HDF5 y accessor para DataArray de xarray.", lang: "es" },
        { title: "Referencia API", url: "/es/api", excerpt: "Tabla de referencia de Quantity, propiedades y métodos.", lang: "es" },
        { title: "Funciones de Módulo", url: "/es/api#module-functions", excerpt: "system_context, propagation_mode, jit, save_state.", lang: "es" },
        { title: "Excepciones", url: "/es/api#exceptions", excerpt: "IncompatibleUnitsError, UnknownUnitError, ConversionError.", lang: "es" },
        { title: "Ejemplos Prácticos", url: "/es/examples", excerpt: "Cinemática, Fuerza, Presión, Temperatura, Péndulo, NumPy, JIT.", lang: "es" },
        { title: "CLI y REPL detalles", url: "/es/cli", excerpt: "Gramática MeasureNote y ejecución por tuberías.", lang: "es" },
        { title: "Historial", url: "/es/changelog", excerpt: "Notas de lanzamiento de la versión v0.1.0 a la v0.1.8.", lang: "es" },
      ];

      const currentLang = isSpanish ? "es" : "en";
      const searchModal = document.getElementById('searchModal');
      const searchTrigger = document.getElementById('searchTrigger');
      const searchClose = document.getElementById('searchClose');
      const searchInput = document.getElementById('searchInput');
      const searchResults = document.getElementById('searchResults');

      let activeIndex = -1;

      function openSearch() {
        searchModal.style.display = 'flex';
        searchInput.focus();
        document.body.style.overflow = 'hidden';
      }

      function closeSearch() {
        searchModal.style.display = 'none';
        searchInput.value = '';
        searchResults.innerHTML = '';
        activeIndex = -1;
        document.body.style.overflow = '';
      }

      searchTrigger?.addEventListener('click', openSearch);
      searchClose?.addEventListener('click', closeSearch);

      // Backdrop close
      searchModal?.addEventListener('click', (e) => {
        if (e.target === searchModal) {
          closeSearch();
        }
      });

      // ESC key close & Ctrl+K open
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && searchModal.style.display === 'flex') {
          closeSearch();
        }
        if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
          e.preventDefault();
          openSearch();
        }
      });

      // Realtime search
      searchInput?.addEventListener('input', () => {
        const query = searchInput.value.toLowerCase().trim();
        if (!query) {
          searchResults.innerHTML = '';
          return;
        }

        const filtered = searchIndex
          .filter(item => item.lang === currentLang)
          .filter(item => item.title.toLowerCase().includes(query) || item.excerpt.toLowerCase().includes(query))
          .slice(0, 8);

        searchResults.innerHTML = filtered.map((item, idx) => `
          <a href="${item.url}" class="search-result-item ${idx === 0 ? 'selected' : ''}">
            <div class="result-title">${item.title}</div>
            <div class="result-excerpt">${item.excerpt}</div>
          </a>
        `).join('');

        activeIndex = filtered.length > 0 ? 0 : -1;
      });

      // Keyboard navigation in results
      searchInput?.addEventListener('keydown', (e) => {
        const items = searchResults.querySelectorAll('.search-result-item');
        if (items.length === 0) return;

        if (e.key === 'ArrowDown') {
          e.preventDefault();
          items[activeIndex]?.classList.remove('selected');
          activeIndex = (activeIndex + 1) % items.length;
          items[activeIndex].classList.add('selected');
          items[activeIndex].scrollIntoView({ block: 'nearest' });
        } else if (e.key === 'ArrowUp') {
          e.preventDefault();
          items[activeIndex]?.classList.remove('selected');
          activeIndex = (activeIndex - 1 + items.length) % items.length;
          items[activeIndex].classList.add('selected');
          items[activeIndex].scrollIntoView({ block: 'nearest' });
        } else if (e.key === 'Enter') {
          e.preventDefault();
          if (activeIndex >= 0 && items[activeIndex]) {
            items[activeIndex].click();
          }
        }
      });
    </script>
  </body>
</html>

<style is:global>
  :root {
    --bg: #0a0d13;
    --bg2: #0f1318;
    --panel: #141920;
    --panel2: #1a2030;
    --text: #e2e8f0;
    --text2: #94a3b8;
    --text3: #64748b;
    --accent: #6366f1;
    --accent2: #818cf8;
    --accent-glow: rgba(99, 102, 241, 0.15);
    --green: #10b981;
    --yellow: #f59e0b;
    --red: #ef4444;
    --border: #1e2535;
    --border2: #252d3d;
    --radius: 8px;
    --radius-lg: 12px;
    --shadow: 0 4px 24px rgba(0, 0, 0, 0.4);
    --topbar-h: 56px;
    --sidebar-w: 260px;
    color-scheme: dark;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    font-size: 15px;
    line-height: 1.7;
    min-height: 100vh;
  }

  /* ── Topbar ── */
  .topbar {
    position: fixed;
    top: 0; left: 0; right: 0;
    height: var(--topbar-h);
    background: rgba(10, 13, 19, 0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    z-index: 100;
  }
  .topbar-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    height: 100%;
    padding: 0 1.5rem;
  }
  .brand {
    display: flex; align-items: center; gap: 0.5rem;
    text-decoration: none; color: var(--text); flex-shrink: 0;
  }
  .brand-logo { border-radius: 6px; }
  .brand-name { font-weight: 700; font-size: 1rem; }
  .brand-badge {
    background: var(--panel2);
    color: var(--text2);
    border: 1px solid var(--border2);
    border-radius: 20px;
    padding: 0.1em 0.5em;
    font-size: 0.7rem;
    font-weight: 500;
  }

  /* ── Search Bar / Button ── */
  .search-trigger {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--panel);
    border: 1px solid var(--border);
    color: var(--text2);
    border-radius: var(--radius);
    padding: 0.4rem 0.8rem;
    font-size: 0.85rem;
    cursor: pointer;
    transition: all 0.2s;
    width: 220px;
    text-align: left;
    margin-left: 1.5rem;
  }
  .search-trigger:hover {
    border-color: var(--accent);
    color: var(--text);
    background: var(--panel2);
  }
  .search-kbd {
    margin-left: auto;
    font-family: inherit;
    font-size: 0.7rem;
    background: var(--border2);
    border: 1px solid var(--border);
    padding: 0.1em 0.4em;
    border-radius: 4px;
    color: var(--text3);
  }

  /* ── Search Modal ── */
  .search-modal-backdrop {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(10, 13, 19, 0.75);
    backdrop-filter: blur(8px);
    z-index: 1000;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding-top: 15vh;
  }
  .search-modal {
    background: var(--panel);
    border: 1px solid var(--border2);
    width: 100%;
    max-width: 580px;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    animation: search-slide 0.2s cubic-bezier(0.16, 1, 0.3, 1);
  }
  @keyframes search-slide {
    from { transform: translateY(-20px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }
  .search-header {
    display: flex;
    align-items: center;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--border2);
    gap: 0.75rem;
  }
  .search-header input {
    flex: 1;
    background: none;
    border: none;
    color: var(--text);
    font-size: 1.05rem;
    outline: none;
    font-family: inherit;
  }
  .search-header input::placeholder {
    color: var(--text3);
  }
  .search-close {
    background: none;
    border: none;
    color: var(--text3);
    cursor: pointer;
    font-size: 1.1rem;
    padding: 0.25rem;
    transition: color 0.15s;
  }
  .search-close:hover {
    color: var(--text);
  }
  .search-results {
    max-height: 380px;
    overflow-y: auto;
    padding: 0.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .search-result-item {
    display: flex;
    flex-direction: column;
    padding: 0.75rem 1rem;
    border-radius: var(--radius);
    text-decoration: none !important;
    transition: background 0.15s;
  }
  .search-result-item:hover, .search-result-item.selected {
    background: var(--panel2);
    outline: none;
  }
  .result-title {
    font-size: 0.925rem;
    font-weight: 600;
    color: var(--accent2);
    margin-bottom: 0.15rem;
  }
  .search-result-item:hover .result-title, .search-result-item.selected .result-title {
    color: var(--text);
  }
  .result-excerpt {
    font-size: 0.8rem;
    color: var(--text2);
    line-height: 1.4;
  }
  .search-footer {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 0.75rem 1.25rem;
    background: var(--bg2);
    border-top: 1px solid var(--border2);
    font-size: 0.75rem;
    color: var(--text3);
  }

  .topbar-nav {
    display: flex; align-items: center; gap: 1rem;
    margin-left: auto;
  }
  .topbar-link, .lang-switcher {
    display: flex; align-items: center; gap: 0.35rem;
    color: var(--text2); text-decoration: none;
    font-size: 0.875rem; font-weight: 500;
    transition: color 0.2s;
  }
  .topbar-link:hover, .lang-switcher:hover { color: var(--accent2); }
  .lang-switcher {
    background: var(--panel2);
    border: 1px solid var(--border2);
    border-radius: var(--radius);
    padding: 0.3rem 0.6rem;
    font-size: 0.8rem;
  }
  .menu-toggle {
    display: none; background: none; border: none;
    color: var(--text); font-size: 1.2rem; cursor: pointer;
    margin-left: auto;
  }

  /* ── Layout ── */
  .layout {
    display: flex;
    padding-top: var(--topbar-h);
    min-height: 100vh;
  }

  /* ── Sidebar ── */
  .sidebar {
    position: fixed;
    top: var(--topbar-h); bottom: 0; left: 0;
    width: var(--sidebar-w);
    overflow-y: auto;
    border-right: 1px solid var(--border);
    background: var(--bg2);
    scrollbar-width: thin;
    scrollbar-color: var(--border2) transparent;
    z-index: 50;
  }
  .sidebar-inner { padding: 1.5rem 1rem; }
  .sidebar-section { margin-bottom: 1.75rem; }
  .sidebar-label {
    color: var(--text3); font-size: 0.7rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.08em;
    margin-bottom: 0.5rem; padding: 0 0.5rem;
  }
  .sidebar-nav { list-style: none; }
  .sidebar-link {
    display: flex; align-items: center; gap: 0.6rem;
    padding: 0.45rem 0.75rem;
    border-radius: var(--radius);
    color: var(--text2); text-decoration: none;
    font-size: 0.875rem; font-weight: 450;
    transition: all 0.15s;
  }
  .sidebar-link:hover { background: var(--panel); color: var(--text); }
  .sidebar-link.active {
    background: var(--accent-glow);
    color: var(--accent2);
    font-weight: 500;
  }
  .nav-icon { font-size: 0.85rem; }

  .install-box {
    display: flex; align-items: center; justify-content: space-between;
    background: var(--panel);
    border: 1px solid var(--border2);
    border-radius: var(--radius);
    padding: 0.6rem 0.75rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    color: var(--accent2);
  }
  .copy-btn {
    background: none; border: none; cursor: pointer;
    color: var(--text3); transition: color 0.2s;
    display: flex; align-items: center;
  }
  .copy-btn:hover { color: var(--text); }

  .sidebar-stats { list-style: none; }
  .sidebar-stats li {
    display: flex; justify-content: space-between; align-items: center;
    padding: 0.3rem 0.5rem;
    border-radius: 4px;
    font-size: 0.8rem;
  }
  .stat-key { color: var(--text3); }
  .stat-val { color: var(--text2); font-weight: 500; }
  .status-beta {
    background: rgba(245, 158, 11, 0.15);
    color: var(--yellow);
    padding: 0.1em 0.4em;
    border-radius: 4px;
    font-size: 0.72rem;
  }

  /* ── Main Content ── */
  .main-content {
    margin-left: var(--sidebar-w);
    flex: 1;
    min-width: 0;
  }
  .content-inner {
    max-width: 900px;
    margin: 0 auto;
    padding: 2.5rem 2.5rem 5rem;
  }

  /* ── Typography ── */
  h1 { font-size: 2.25rem; font-weight: 700; line-height: 1.2; margin-bottom: 0.75rem; color: var(--text); }
  h2 { font-size: 1.5rem; font-weight: 600; line-height: 1.3; margin: 2.5rem 0 0.75rem; color: var(--text); border-bottom: 1px solid var(--border); padding-bottom: 0.5rem; }
  h3 { font-size: 1.15rem; font-weight: 600; margin: 1.75rem 0 0.5rem; color: var(--text); }
  h4 { font-size: 1rem; font-weight: 600; margin: 1.25rem 0 0.35rem; color: var(--text2); }
  p { margin-bottom: 1rem; color: var(--text2); }
  a { color: var(--accent2); text-decoration: none; }
  a:hover { text-decoration: underline; }
  ul, ol { padding-left: 1.5rem; margin-bottom: 1rem; color: var(--text2); }
  li { margin-bottom: 0.3rem; }
  strong { color: var(--text); font-weight: 600; }

  /* ── Code ── */
  pre {
    background: var(--panel);
    border: 1px solid var(--border2);
    border-radius: var(--radius-lg);
    padding: 1.25rem 1.5rem;
    overflow-x: auto;
    margin: 1rem 0 1.5rem;
    font-size: 0.855rem;
    line-height: 1.65;
    position: relative;
  }
  code {
    font-family: 'JetBrains Mono', ui-monospace, monospace;
    font-size: 0.875em;
  }
  p code, li code, td code {
    background: var(--panel);
    border: 1px solid var(--border);
    padding: 0.1em 0.4em;
    border-radius: 4px;
    color: var(--accent2);
    font-size: 0.85em;
  }

  .pre-copy-btn {
    position: absolute; top: 0.75rem; right: 0.75rem;
    background: var(--panel2); border: 1px solid var(--border2);
    border-radius: 4px; padding: 0.3rem 0.4rem;
    color: var(--text3); cursor: pointer;
    opacity: 0; transition: opacity 0.2s, color 0.2s;
    display: flex; align-items: center;
  }
  pre:hover .pre-copy-btn { opacity: 1; }
  .pre-copy-btn:hover { color: var(--text); }

  /* ── Cards ── */
  .card {
    background: var(--panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 1.25rem 1.5rem;
    margin-bottom: 1rem;
    transition: border-color 0.2s;
  }
  .card:hover { border-color: var(--border2); }
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1rem;
    margin: 1rem 0;
  }

  /* ── Badges / Callouts ── */
  .callout {
    border-radius: var(--radius);
    padding: 0.875rem 1.25rem;
    margin: 1rem 0;
    border-left: 3px solid;
    font-size: 0.9rem;
  }
  .callout p { margin: 0; }
  .callout-info { background: rgba(99, 102, 241, 0.08); border-color: var(--accent); }
  .callout-warning { background: rgba(245, 158, 11, 0.08); border-color: var(--yellow); }
  .callout-danger { background: rgba(239, 68, 68, 0.08); border-color: var(--red); }
  .callout-success { background: rgba(16, 185, 129, 0.08); border-color: var(--green); }

  /* ── Tables ── */
  table { width: 100%; border-collapse: collapse; margin: 1rem 0 1.5rem; font-size: 0.875rem; }
  th { background: var(--panel2); color: var(--text); font-weight: 600; text-align: left; padding: 0.6rem 1rem; border-bottom: 1px solid var(--border2); }
  td { padding: 0.55rem 1rem; border-bottom: 1px solid var(--border); color: var(--text2); }
  tr:last-child td { border-bottom: none; }
  tr:hover td { background: var(--panel); }

  /* ── Badges ── */
  .badge {
    display: inline-block;
    padding: 0.15em 0.55em;
    border-radius: 20px;
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }
  .badge-blue { background: rgba(99,102,241,0.2); color: var(--accent2); }
  .badge-green { background: rgba(16,185,129,0.2); color: var(--green); }
  .badge-yellow { background: rgba(245,158,11,0.2); color: var(--yellow); }
  .badge-gray { background: var(--panel2); color: var(--text3); }

  /* ── Page header ── */
  .page-header {
    margin-bottom: 2.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--border);
  }
  .page-header .kicker {
    color: var(--accent2); font-size: 0.8rem; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.08em;
    margin-bottom: 0.5rem;
  }
  .page-header p { font-size: 1.05rem; color: var(--text2); margin-bottom: 0; }

  /* ── Steps ── */
  .steps { counter-reset: step; list-style: none; padding: 0; }
  .steps li {
    counter-increment: step;
    display: flex; gap: 1rem;
    margin-bottom: 1.75rem;
    position: relative;
  }
  .steps li::before {
    content: counter(step);
    flex-shrink: 0;
    width: 28px; height: 28px;
    background: var(--accent-glow);
    border: 1px solid var(--accent);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: var(--accent2); font-size: 0.78rem; font-weight: 700;
    margin-top: 0.1rem;
  }

  /* ── Tabs ── */
  .tabs { margin: 1.5rem 0; }
  .tab-list { display: flex; gap: 0; border-bottom: 1px solid var(--border); margin-bottom: 0; }
  .tab-btn {
    padding: 0.6rem 1.2rem;
    background: none; border: none; cursor: pointer;
    color: var(--text3); font-size: 0.875rem; font-weight: 500;
    border-bottom: 2px solid transparent; margin-bottom: -1px;
    font-family: inherit; transition: all 0.15s;
  }
  .tab-btn.active { color: var(--accent2); border-bottom-color: var(--accent2); }
  .tab-btn:hover:not(.active) { color: var(--text2); }
  .tab-panel { display: none; padding: 0; }
  .tab-panel.active { display: block; }

  /* ── Desktop Wide Screen / Dead Space Reduction ── */
  @media (min-width: 1200px) {
    .content-inner:has(.toc-card) {
      max-width: 1200px;
      padding-right: 320px;
      position: relative;
    }
    .toc-card {
      position: absolute;
      top: 2.5rem;
      right: 2.5rem;
      width: 270px;
      background: transparent;
      border: none;
      padding: 0;
      margin: 0;
    }
    .toc-list {
      columns: 1 !important;
      gap: 0.5rem;
    }
    .toc-list li {
      border-left: 2px solid var(--border);
      padding-left: 0.75rem;
      margin-bottom: 0.5rem;
    }
    .toc-list a {
      color: var(--text2);
      font-size: 0.825rem;
      display: block;
      transition: color 0.15s;
    }
    .toc-list a:hover {
      color: var(--accent2);
      text-decoration: none;
    }
  }

  /* ── Mobile ── */
  @media (max-width: 768px) {
    .menu-toggle { display: flex; align-items: center; }
    .topbar-nav { display: none; }
    .search-trigger {
      width: 100px;
      margin-left: auto;
      margin-right: 0.5rem;
      padding: 0.35rem 0.6rem;
      font-size: 0.8rem;
    }
    .search-kbd { display: none; }
    .sidebar {
      transform: translateX(-100%);
      transition: transform 0.25s;
    }
    .sidebar.open { transform: translateX(0); }
    .main-content { margin-left: 0; }
    .content-inner { padding: 1.5rem 1rem 4rem; }
    h1 { font-size: 1.75rem; }
  }
</style>
